import { styled } from 'styled-components'

/** AcquiredAugmentsModal 카드 상단 ID 배지 */
const AugmentBadge = styled.span`
  font-size: 11px;
  font-weight: 600;
  color: rgba(0, 0, 0, 0.5);
  background: rgba(255, 255, 255, 0.5);
  padding: 2px 8px;
  border-radius: 8px;
`

export default AugmentBadge
