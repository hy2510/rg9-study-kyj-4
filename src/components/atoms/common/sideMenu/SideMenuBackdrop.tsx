import styled from 'styled-components'

const SideMenuBackdrop = styled.button`
  cursor: pointer;
  position: fixed;
  inset: 0;
  z-index: 150;
  border: none;
  background: rgba(0, 0, 0, 0.25);
`

export default SideMenuBackdrop
