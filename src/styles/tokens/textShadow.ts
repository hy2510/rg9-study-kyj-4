/**
 * 기본 텍스트 그림자 — styled-components `text-shadow` 등에서 공통 사용
 */
export const TEXT_SHADOW_DEFAULT = '0 2px 0 rgba(0, 0, 0, 0.35)' as const

/**
 * 부드러운 텍스트 그림자 — 작은 텍스트의 가독성 보강용
 */
export const TEXT_SHADOW_SOFT = '0 1px 4px rgba(0, 0, 0, 0.25)' as const
